const addComment = require('./addComment')
const dltComment = require('./dltCommentById')
const getAllComments = require('./getAllComments')

module.exports = {
  addComment,
  dltComment,
  getAllComments,
}
